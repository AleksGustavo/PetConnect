import 'package:flutter/material.dart';

/// Placeholder — formulário completo de cadastro (RF01-RF03) entra em uma
/// próxima etapa, seguindo o mesmo estilo visual do LoginScreen.
class CadastroScreen extends StatelessWidget {
  const CadastroScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cadastre-se')),
      body: const Center(child: Text('Formulário de cadastro — em breve')),
    );
  }
}
