import 'package:flutter/material.dart';

/// Placeholder — formulário de recuperação de senha (RF05) entra em uma
/// próxima etapa, seguindo o mesmo estilo visual do LoginScreen.
class EsqueciSenhaScreen extends StatelessWidget {
  const EsqueciSenhaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Esqueci minha senha')),
      body: const Center(child: Text('Formulário de recuperação — em breve')),
    );
  }
}
